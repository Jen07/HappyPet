


document.addEventListener( 'DOMContentLoaded', function () {
	new Splide( '#image-slider', {
		rewind     : true,
		heightRatio: 0.6,
		pagination : false,
		autoplay    : true,
		pauseOnHover: false,
		
	} ).mount();
} );
